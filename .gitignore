# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results
playwright-report
blob-report
playwright/.cache

# Real media used for one-off measurements, never committed
tmp-media/

# The API key the assistant uses. Never commit this.
.env
.env.local

# The live bridge writes the current timeline here for an agent to read.
live/

# The transcription sidecar lives here; it is a big download, not source.
.venv/
